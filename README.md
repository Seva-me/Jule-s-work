# Monorepo

This is a monorepo with two workspaces:

- `backend`: The backend server.
- `frontend`: The frontend application.

## Getting Started

To get started, install the dependencies from the root directory:

```bash
npm install
```

You can then run the `start` script for each workspace:

```bash
npm start -w backend
npm start -w frontend
```
